'use strict'

const events = require('./events')

$(() => {
  $('#box0').one('click', events.onClick)
  $('#box1').one('click', events.onClick)
  $('#box2').one('click', events.onClick)
  $('#box3').one('click', events.onClick)
  $('#box4').one('click', events.onClick)
  $('#box5').one('click', events.onClick)
  $('#box6').one('click', events.onClick)
  $('#box7').one('click', events.onClick)
  $('#box8').one('click', events.onClick)
  $('#sign-up-form').on('submit', events.onSignUp)
  $('#sign-in-form').on('submit', events.onSignIn)
  $('#change-password-form').on('submit', events.onChangePassword)
  $('#sign-out').on('click', events.onSignOut)
  $('#newGame').on('click', events.onNewGame)
  $('#getGameData').on('click', events.onGetGameData)
  $('#getGameData').hide()
  $('#getThisGameData').on('click', events.onGetThisGameData)
  $('#getThisGameData').hide()
  $('#updateMoveTest').on('click', events.onUpdateMove)
  $('#updateMoveTest').hide()
  $('#box0').one('click', events.onUpdateMove)
  $('#box1').one('click', events.onUpdateMove)
  $('#box2').one('click', events.onUpdateMove)
  $('#box3').one('click', events.onUpdateMove)
  $('#box4').one('click', events.onUpdateMove)
  $('#box5').one('click', events.onUpdateMove)
  $('#box6').one('click', events.onUpdateMove)
  $('#box7').one('click', events.onUpdateMove)
  $('#box8').one('click', events.onUpdateMove)
  $('#change-password-form').hide()
  $('#sign-out').hide()
  $('#sign-up-form').hide()
  $('#get-sign-up').on('click', events.onShowSignUp)
  $('#get-user-stats').on('click', events.onGetUserStats)
  $('#get-change-password').on('click', events.onGetChangePassword)
  $('#get-change-password').hide()
  $('.change-confirmation').hide()
  $('.invalid-credentials').hide()
  $('.passwords-match').hide()
  $('.email-taken').hide()
  $('#sign-up-back').hide()
  $('#sign-up-back').on('click', events.onSignUpBack)
  $('.enter-email').hide()
  $('#change-password-back').hide()
  $('#change-password-back').on('click', events.onChangePasswordBack)
  $('#reset-score').on('click', events.onResetScore)
  $('.play-area').hide()
  $('.scoreboard').hide()
  $('#reset-score').hide()
  $('.user-stats').hide()
  $('#get-user-stats').hide()
  $('#newGame').hide()
  $('.turn-notifier').hide()
  $('.reset-notifier').hide()
  $('.stats-notifier').hide()
  $('.start-notifier').hide()
})
